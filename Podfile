# Uncomment the next line to define a global platform for your project
platform :osx, '10.13'

target 'Focusrite Midi Control' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Focusrite Midi Control
  pod 'SwiftSocket', '~> 2.0.2'
  pod 'SWXMLHash', '~> 4.0.0'

  target 'Focusrite Midi ControlTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Focusrite Midi ControlUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
